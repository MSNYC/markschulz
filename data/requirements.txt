# Achievement Extraction Tool Dependencies

# Claude API
anthropic>=0.39.0

# Environment variables
python-dotenv>=1.0.0

# Document reading
PyPDF2>=3.0.0
python-docx>=1.1.0

# Image-based PDF processing (for scanned/image PDFs)
pdf2image>=1.16.0
Pillow>=10.0.0

# Already available (standard library)
# json, sys, os, pathlib, datetime, shutil, base64

# System dependencies (install separately):
#
# 1. poppler-utils (required by pdf2image for PDF processing)
#   macOS: brew install poppler
#   Ubuntu/Debian: sudo apt-get install poppler-utils
#   Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases/
#
# 2. antiword (required for legacy .doc file support)
#   macOS: brew install antiword
#   Ubuntu/Debian: sudo apt-get install antiword
#   Windows: Download from http://www.winfield.demon.nl/
#   Note: antiword is optional - you can convert .doc to .docx manually instead
